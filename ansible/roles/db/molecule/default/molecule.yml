---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: virtualbox
platforms:
  - name: instance
    box: ubuntu/xenial64
    memory: 1024
    cpus: 2
provisioner:
  name: ansible
verifier:
  name: testinfra
